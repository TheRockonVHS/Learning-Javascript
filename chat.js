```javascript
your code here
```;

const cogsItem = {
  ink_price_per_sf: '',
  vinyl_price_per_sf: '',
  lam_price_per_sf: '',
  app_tape_price_per_sf: '',
  substrate_price_per_sf: '',
  width: '',
  height: '',
  qty: '',
};

function getInputs() {}
function convertToNumbers() {}
function calculateCOGS() {}
function renderPrice() {}

if (Number.isNaN(value) || value <= 0 ) {
  return;
}

// ```javascript
// 1
arry = arry.filter(item);

// 2
// Answer: need to clear the DOM and the render the updated array

// 3
// prevents the default event from running which is submitting the form

// 4
// need to make that the input is Number so I can properly compare numbers. A string to number type doesn't compare right.